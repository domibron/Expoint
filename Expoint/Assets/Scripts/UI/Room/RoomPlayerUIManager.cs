using System.Collections;
using System.Collections.Generic;
using Mirror;
using UnityEngine;

public class RoomPlayerUIManager : MonoBehaviour
{
	NetworkRoomManager NetRoomManager;
	// Start is called before the first frame update
	void Start()
	{
		NetRoomManager = NetworkManager.singleton as NetworkRoomManager;
	}

	// Update is called once per frame
	void Update()
	{
		for
	}
}
